name: Windows Standalone Binary

# Builds a standalone reveal.exe on every GitHub Release.
# Output: reveal-<version>-windows-x64.zip attached to the release.
#
# Uses PyInstaller (--onedir), NOT Nuitka. Nuitka 4.x mis-compiles a native
# dependency in reveal's graph on Windows, producing a reveal.exe that
# fail-fasts at import (0xC0000409, zero output) — see BACK-495. PyInstaller
# bundles the real CPython interpreter and copies the .pyd/.dll files as-is
# (no recompilation), which builds a working binary; validated end-to-end
# (reveal.exe --version -> "reveal <version>").
#
# CRITICAL: always build --onedir (folder), never --onefile.
# --onefile extracts to a temp dir at launch — an AV heuristic that has
# caused signing certificates to be suspended. --onedir avoids that.
#
# To enable code signing, enroll in Microsoft Artifact Signing
# (Azure/artifact-signing-action@v2; self-employed US/CA, ~$10/mo)
# and set these repository secrets:
#   AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET,
#   AZURE_CODE_SIGNING_ACCOUNT_NAME, AZURE_CERT_PROFILE_NAME

on:
  release:
    types: [published]
  workflow_dispatch:  # manual test run — upload step below stays release-only

permissions:
  contents: write   # upload release assets
  id-token: write   # attest-build-provenance

jobs:
  build-windows:
    name: Build Windows standalone binary
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install reveal + PyInstaller
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[git,mcp]"
          pip install pyinstaller

      - name: Build with PyInstaller --onedir
        shell: bash
        run: |
          pyinstaller --noconfirm --onedir --name reveal \
            --collect-all reveal \
            --collect-all tree_sitter \
            --collect-all tree_sitter_language_pack \
            --collect-all pygit2 \
            --collect-all certifi \
            --collect-all jsonschema_specifications \
            pyinstaller_entry.py

      - name: Smoke test the binary
        shell: cmd
        run: dist\reveal\reveal.exe --version

      - name: Package as zip
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}"
          $zipName = "reveal-$version-windows-x64.zip"
          # PyInstaller --onedir --name reveal writes the bundle to dist/reveal/.
          Compress-Archive -Path dist/reveal/* -DestinationPath $zipName
          echo "ZIP_NAME=$zipName" >> $env:GITHUB_ENV

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: ${{ env.ZIP_NAME }}

      # Code signing — uncomment once enrolled in Microsoft Artifact Signing.
      # Signing alone does NOT clear SmartScreen; reputation accrues per-hash
      # over weeks–months of real download volume. Start building it now.
      #
      # Enroll at: https://learn.microsoft.com/en-us/azure/trusted-signing/
      # Required secrets: AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET,
      #   AZURE_CODE_SIGNING_ACCOUNT_NAME, AZURE_CERT_PROFILE_NAME
      # Action: Azure/artifact-signing-action@v2 (v2.0.0, May 2026)
      # azure-code-signing-action is 404; trusted-signing-action redirects here.
      #
      # - name: Sign binary (Microsoft Artifact Signing)
      #   uses: Azure/artifact-signing-action@v2
      #   with:
      #     azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
      #     azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
      #     azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
      #     endpoint: https://eus.codesigning.azure.net/
      #     signing-account-name: ${{ secrets.AZURE_CODE_SIGNING_ACCOUNT_NAME }}
      #     certificate-profile-name: ${{ secrets.AZURE_CERT_PROFILE_NAME }}
      #     files-folder: dist\reveal
      #     files-folder-filter: exe
      #     file-digest: SHA256
      #     timestamp-rfc3161: http://timestamp.acs.microsoft.com
      #     timestamp-digest: SHA256

      - name: Upload zip to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ZIP_NAME }}
