# Generated by tests/test_subcommand_flag_matrix.py --write (BACK-1362).
# `via` is derived from code; `review`/`reason` are hand-written for declared cells:
# honored (say how you proved it, ideally a PROBES entry) or not-applicable (say why).
flags:
- verbose
- all
- since
- until
- respect_gitignore
paths:
- subcommand
subcommands:
  architecture:
    all:
      reason: --all is not declared on `reveal architecture`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal architecture`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal architecture`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'BACK-1362: fixed this session -- lifts --top to UNLIMITED_TOP; PROBES
        entry proves an output diff'
      review: honored
      via:
      - declared-read
  check:
    all:
      reason: --all is not declared on `reveal check`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: forwarded into cli/file_checker.py:handle_recursive_check/handle_profile_rules
        (BACK-1362 _CROSS_FILE_READERS); PROBES entry proves an output diff
      review: honored
      via:
      - declared-read
    since:
      reason: --since is not declared on `reveal check`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal check`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: check has no truncated section for --verbose to expand -- its analog
        is --max-items (opt-in, unbounded by default, BACK-1181), not a default cap
      review: not-applicable
      via:
      - declared-unused
  contracts:
    all:
      reason: --all is not declared on `reveal contracts`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal contracts`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal contracts`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: contracts:// output has no --top/limit concept anywhere to lift; confirmed
        live, no truncation exists to expand
      review: not-applicable
      via:
      - declared-unused
  deps:
    all:
      reason: --all is not declared on `reveal deps`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal deps`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal deps`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'BACK-1362: fixed this session -- lifts --top to UNLIMITED_TOP; PROBES
        entry proves an output diff'
      review: honored
      via:
      - declared-read
  dev:
    all:
      reason: --all is not declared on `reveal dev`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: --respect_gitignore is not declared on `reveal dev`'s own parser --
        argparse rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    since:
      reason: --since is not declared on `reveal dev`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal dev`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: --verbose is not declared on `reveal dev`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
  health:
    all:
      reason: --all is not declared on `reveal health`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied by apply_global_flags; health collects its files
        with collect_files_to_check, which follows the switch (test_gitignore_oracle.py,
        test_cli_health.py)'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal health`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal health`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: health has no truncated display section; its only cap (_HEALTH_MAX_FILES)
        is a memory safety guard before invoking `reveal check`, not something --verbose
        should lift
      review: not-applicable
      via:
      - declared-unused
  hotspots:
    all:
      reason: --all is not declared on `reveal hotspots`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal hotspots`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal hotspots`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'BACK-1362: fixed this session -- lifts --top to UNLIMITED_TOP (same
        meaning hotspots://...?all=true already has); PROBES entry proves an output
        diff'
      review: honored
      via:
      - declared-read
  offline:
    all:
      reason: --all is not declared on `reveal offline`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: --respect_gitignore is not declared on `reveal offline`'s own parser
        -- argparse rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    since:
      reason: --since is not declared on `reveal offline`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal offline`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: --verbose is not declared on `reveal offline`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
  overview:
    all:
      reason: 'pre-existing (BACK-1226): lifts --top to UNLIMITED_TOP via inject_query_flags;
        PROBES entry proves an output diff'
      review: honored
      via:
      - declared-read
    respect_gitignore:
      reason: 'pre-existing: OverviewAdapter.CLI_QUERY_FLAGS declares respect_gitignore,
        forwarded via the shared inject_query_flags seam (BACK-1377)'
      review: honored
      via:
      - declared-read
    since:
      reason: --since is not declared on `reveal overview`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal overview`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'pre-existing (BACK-1226): same effect as --all here, lifts --top to
        UNLIMITED_TOP'
      review: honored
      via:
      - declared-read
  pack:
    all:
      reason: --all is not declared on `reveal pack`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: 'pre-existing: threaded into the PackAdapter query string and read by
        the adapter'
      review: honored
      via:
      - declared-read
    until:
      reason: --until is not declared on `reveal pack`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'pre-existing: passed to PackRenderer.render_structure(verbose=...)'
      review: honored
      via:
      - declared-read
  review:
    all:
      reason: --all is not declared on `reveal review`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied by apply_global_flags; review collects its files
        with collect_files_to_check, which follows the switch (test_gitignore_oracle.py,
        test_cli_health.py)'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal review`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal review`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'pre-existing: passed through to _render_report/_render_violations_section'
      review: honored
      via:
      - declared-read
  scaffold:
    all:
      reason: --all is not declared on `reveal scaffold`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: --respect_gitignore is not declared on `reveal scaffold`'s own parser
        -- argparse rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    since:
      reason: --since is not declared on `reveal scaffold`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal scaffold`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: --verbose is not declared on `reveal scaffold`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
  surface:
    all:
      reason: --all is not declared on `reveal surface`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal surface`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal surface`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: surface's --top already defaults to None (unlimited) -- there is no
        default cap for --verbose to lift
      review: not-applicable
      via:
      - declared-unused
  testability:
    all:
      reason: --all is not declared on `reveal testability`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal testability`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal testability`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: 'BACK-1362: fixed this session -- lifts --top to UNLIMITED_TOP; PROBES
        entry proves an output diff'
      review: honored
      via:
      - declared-read
  trace:
    all:
      reason: --all is not declared on `reveal trace`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: 'BACK-1386: applied to every walker by apply_global_flags; PROBES entry
        proves an output diff'
      review: honored
      via:
      - declared-global
    since:
      reason: --since is not declared on `reveal trace`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    until:
      reason: --until is not declared on `reveal trace`'s own parser -- argparse rejects
        it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
    verbose:
      reason: --verbose is not declared on `reveal trace`'s own parser -- argparse
        rejects it loudly (unrecognized arguments), not a silent gap
      review: not-applicable
      via: []
