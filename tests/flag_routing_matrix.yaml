# Generated by tests/test_flag_routing_matrix.py --write (BACK-1362).
# `via` is derived from code; `review`/`reason` on cells with empty `via` are
# hand-written: not-applicable (say why) or unreviewed (flag is accepted and
# silently ignored -- decide: honor it, reject it, or mark not-applicable).
adapters:
  architecture:
    all:
      via:
      - routing-special
    respect_gitignore:
      via:
      - declared
    since:
      reason: module/package dependency graph, no per-item timestamp anywhere in the
        file (architecture.py:530); existing warn_unsupported note is correct
      review: not-applicable
      via: []
    until:
      reason: module/package dependency graph, no per-item timestamp anywhere in the
        file (architecture.py:530); existing warn_unsupported note is correct
      review: not-applicable
      via: []
    verbose:
      via:
      - routing-special
  ast:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      reason: returns AST nodes (functions/classes); no per-node date field exists
        anywhere in ast/*.py; confirmed live -- output unchanged with --since
      review: not-applicable
      via: []
    until:
      reason: returns AST nodes (functions/classes); no per-node date field exists
        anywhere in ast/*.py; confirmed live -- output unchanged with --since
      review: not-applicable
      via: []
    verbose:
      reason: any unrecognized ?key becomes a structural-attribute filter (generic
        filter mechanism, adapter.py); verbose==True filter live-confirmed to match
        0 AST nodes, not a detail toggle
      review: not-applicable
      via: []
  autossl:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      reason: resource is a fixed AutoSSL log directory (adapter.py:25 AUTOSSL_LOG_DIR),
        not a git-tracked project checkout; no .gitignore concept applies
      review: not-applicable
      via: []
    since:
      review: unreviewed
      via: []
    until:
      review: unreviewed
      via: []
    verbose:
      reason: get_structure(summary=False, ...) already defaults to full per-user
        detail; no code hook for a distinct verbose concept (adapter.py:417-420)
      review: not-applicable
      via: []
  calls:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      reason: caller/callee graph and confidence scores; mtime_ns is used only as
        a cache key (invalidation), never exposed as a per-result field or filterable
        (calls/index.py)
      review: not-applicable
      via: []
    until:
      reason: caller/callee graph and confidence scores; mtime_ns is used only as
        a cache key (invalidation), never exposed as a per-result field or filterable
        (calls/index.py)
      review: not-applicable
      via: []
    verbose:
      reason: 'CLI_QUERY_FLAGS declares only ''all''; render_structure(result, format)
        has no verbose param; confirmed live: ''Unknown query param verbose'' warning'
      review: not-applicable
      via: []
  classify:
    all:
      reason: get_structure lists every file already (classify.py:216-249); no [:N]
        slice to lift
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: static content classification (vendored/generated/etc), no date field
        anywhere in the file (classify.py)
      review: not-applicable
      via: []
    until:
      reason: static content classification (vendored/generated/etc), no date field
        anywhere in the file (classify.py)
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(**kwargs) has no query validation and no detail hook;
        confirmed live: ?verbose=1 output byte-identical, no warning (classify.py:216)'
      review: not-applicable
      via: []
  claude:
    all:
      via:
      - adapter-reads-args
    respect_gitignore:
      reason: enumerates session .jsonl logs under CONVERSATION_BASE (~/.claude/projects/,
        adapter.py:728,397-469) -- a fixed session-log directory, no .gitignore semantics
      review: not-applicable
      via: []
    since:
      via:
      - adapter-reads-args
    until:
      via:
      - adapter-reads-args
    verbose:
      via:
      - adapter-reads-args
  codex:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      reason: reads Codex's own SQLite-backed session store (adapter.py:213, sqlite3),
        not a filesystem tree walk
      review: not-applicable
      via: []
    since:
      via:
      - declared
    until:
      via:
      - declared
    verbose:
      reason: 'get_structure(**kwargs) has no per-item detail toggle; confirmed live:
        ''Unknown query param verbose'' warning (adapter.py:213). Distinct from BACK-1384
        (codex''s all-gap is a hardcoded per-session match count, not a detail concern)'
      review: not-applicable
      via: []
  contracts:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: interface/type-alias/abstract-class extraction, no timestamp field anywhere;
        only 'since' hits are unrelated English prose
      review: not-applicable
      via: []
    until:
      reason: interface/type-alias/abstract-class extraction, no timestamp field anywhere;
        only 'since' hits are unrelated English prose
      review: not-applicable
      via: []
    verbose:
      reason: 'render_structure(result, format) has no verbose param, no detail-level
        concept; confirmed live: ''Unknown query param verbose'' warning (contracts.py:1009).
        Distinct from BACK-1384 (contracts'' all-gap is impls[:5], a count issue)'
      review: not-applicable
      via: []
  cpanel:
    all:
      reason: no result-count query param; domain/ssl/acl-check listings return every
        row (cpanel/adapter.py:704-717)
      review: not-applicable
      via: []
    respect_gitignore:
      reason: queries a live cPanel account via socket/API (adapter.py:624), not a
        filesystem tree walk
      review: not-applicable
      via: []
    since:
      reason: cert audit exposes only forward-looking days_until_expiry/not_after
        (cpanel/adapter.py:261-320), never an occurrence date since/until could bound
      review: not-applicable
      via: []
    until:
      reason: cert audit exposes only forward-looking days_until_expiry/not_after
        (cpanel/adapter.py:261-320), never an occurrence date since/until could bound
      review: not-applicable
      via: []
    verbose:
      reason: 'no query-string parsing at all -- the whole string after ''//'' is
        parsed as a username; confirmed live: ''?verbose=1'' rejected as an invalid
        username (adapter.py:624)'
      review: not-applicable
      via: []
  depends:
    all:
      reason: top_n is None unless the caller passes ?top=N; unclamped by default
        (depends.py:523-524)
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: static import/dependency graph; no per-file change-date is read anywhere
        in the file
      review: not-applicable
      via: []
    until:
      reason: static import/dependency graph; no per-file change-date is read anywhere
        in the file
      review: not-applicable
      via: []
    verbose:
      via:
      - declared
  deps:
    all:
      via:
      - routing-special
    respect_gitignore:
      via:
      - declared
    since:
      reason: dependency listing (deps.py:432), no date/timestamp anywhere in the
        file
      review: not-applicable
      via: []
    until:
      reason: dependency listing (deps.py:432), no date/timestamp anywhere in the
        file
      review: not-applicable
      via: []
    verbose:
      via:
      - routing-special
  diff:
    all:
      reason: output is "what changed" (additions/removals/modifications), not a ranked/truncatable
        list
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: diffs two explicitly-given refs/files (diff/adapter.py:118); not a filterable
        list of dated items -- 'modified' elsewhere in the file is an unrelated diff-summary
        count key
      review: not-applicable
      via: []
    until:
      reason: diffs two explicitly-given refs/files (diff/adapter.py:118); not a filterable
        list of dated items -- 'modified' elsewhere in the file is an unrelated diff-summary
        count key
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(**kwargs) has no query-string splitting; confirmed live:
        ''?verbose=1'' glued onto the literal path and fails as a nonexistent file
        (adapter.py:118)'
      review: not-applicable
      via: []
  domain:
    all:
      reason: single-domain detail view; only per-field string previews exist, not
        a results list
      review: not-applicable
      via: []
    respect_gitignore:
      reason: does DNS/SSL lookups for one domain name (adapter.py:608); already rejects
        all query params with an explicit warning, confirmed live
      review: not-applicable
      via: []
    since:
      reason: WHOIS/DNS/SSL lookups; only date-shaped fields are forward-looking expiration_date/days_until_expiry,
        same class as cpanel/ssl/letsencrypt
      review: not-applicable
      via: []
    until:
      reason: WHOIS/DNS/SSL lookups; only date-shaped fields are forward-looking expiration_date/days_until_expiry,
        same class as cpanel/ssl/letsencrypt
      review: not-applicable
      via: []
    verbose:
      reason: 'no query-string channel by design; confirmed live: adapter itself prints
        ''domain:// does not support query parameters -- ignoring'' (adapter.py:608)'
      review: not-applicable
      via: []
  env:
    all:
      reason: get_structure returns every var already (env.py:302-341); confirmed
        live, output identical
      review: not-applicable
      via: []
    respect_gitignore:
      reason: reads os.environ directly (env.py:302) -- process environment variables,
        no filesystem walk
      review: not-applicable
      via: []
    since:
      reason: lists live env vars (env.py:302) -- a point-in-time snapshot, no per-var
        date field
      review: not-applicable
      via: []
    until:
      reason: lists live env vars (env.py:302) -- a point-in-time snapshot, no per-var
        date field
      review: not-applicable
      via: []
    verbose:
      reason: 'no query-string parsing at all -- anything after ''?'' is treated as
        a literal var name; confirmed live: ''?verbose=1'' (and even the universal
        ?sort=) both error ''Element not found'' (env.py:302)'
      review: not-applicable
      via: []
  git:
    all:
      via:
      - declared
    respect_gitignore:
      reason: 'operates entirely via git history/blame (pygit2), never walks the working-tree
        filesystem directly (grep for os.walk/rglob/gitignore across adapters/git/
        returns nothing); untracked/ignored files never appear in git log/blame output
        by definition, confirmed live: --no-gitignore prints the standard unsupported
        note and output is identical to the default'
      review: not-applicable
      via: []
    since:
      via:
      - declared
    until:
      via:
      - declared
    verbose:
      via:
      - declared
  help:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      reason: self-introspection over reveal's own static help/schema data (help.py:615);
        the rglob calls at :518,543,550 walk reveal's own fixed docs/ tree, not a
        caller-supplied project path
      review: not-applicable
      via: []
    since:
      reason: static, hand-written help/example data (help.py:615), no date field
        anywhere
      review: not-applicable
      via: []
    until:
      reason: static, hand-written help/example data (help.py:615), no date field
        anywhere
      review: not-applicable
      via: []
    verbose:
      reason: 'top-level get_structure(**kwargs) has no query parsing; confirmed live:
        ''?verbose=1'' errors as an unknown element (help.py:615). help://search''s
        one real gap (hits[:20] cap) is BACK-1383, a count issue not a detail issue'
      review: not-applicable
      via: []
  hotspots:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      reason: function-level complexity/length hotspots; no git-churn integration
        in this adapter (unlike stats://, which does have one) -- no per-item date
        exists
      review: not-applicable
      via: []
    until:
      reason: function-level complexity/length hotspots; no git-churn integration
        in this adapter (unlike stats://, which does have one) -- no per-item date
        exists
      review: not-applicable
      via: []
    verbose:
      reason: 'CLI_QUERY_FLAGS declares only ''all''; render_structure(result, format,
        top, test_index) has no verbose param; confirmed live: ''Unknown query param
        verbose'' warning (hotspots.py:296)'
      review: not-applicable
      via: []
  imports:
    all:
      reason: ?rank= is unclamped by default (imports.py:207-210); the one real cap
        (groups[:5]) belongs to --verbose, already declared
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: import-graph/cycle/violation detection; mtime_ns is used only as a cache-fingerprint
        component, never surfaced as a queryable per-result field
      review: not-applicable
      via: []
    until:
      reason: import-graph/cycle/violation detection; mtime_ns is used only as a cache-fingerprint
        component, never surfaced as a queryable per-result field
      review: not-applicable
      via: []
    verbose:
      via:
      - declared
  json:
    all:
      reason: no default cap found in adapter or legacy modes; confirmed live, output
        identical
      review: not-applicable
      via: []
    respect_gitignore:
      reason: operates on one JSON file's parsed structure (adapter.py:48,64), not
        a directory walk
      review: not-applicable
      via: []
    since:
      reason: generic JSON query/filter engine with no canonical date field; a data
        file's own date field (if any) is already filterable via the generic ?field>value=
        syntax
      review: not-applicable
      via: []
    until:
      reason: generic JSON query/filter engine with no canonical date field; a data
        file's own date field (if any) is already filterable via the generic ?field>value=
        syntax
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(**kwargs) has no query validation; confirmed live: ?verbose=1
        output byte-identical, no warning -- output shape (type/path/value) is already
        fixed regardless of query (adapter.py:94)'
      review: not-applicable
      via: []
  letsencrypt:
    all:
      reason: cert inventory is meant to be exhaustive (orphan/duplicate detection
        needs every cert); no cap in code
      review: not-applicable
      via: []
    respect_gitignore:
      reason: walks a fixed certbot config directory (adapter.py:250, iterdir/glob
        at :65,83,159,166) -- infra config, not a git-tracked checkout
      review: not-applicable
      via: []
    since:
      reason: only not_after (forward-looking expiry) per cert, no issuance/occurrence
        date -- same class as cpanel/domain/ssl
      review: not-applicable
      via: []
    until:
      reason: only not_after (forward-looking expiry) per cert, no issuance/occurrence
        date -- same class as cpanel/domain/ssl
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(...) has no detail-level concept; confirmed live: ''Unknown
        query param verbose'' warning (adapter.py:250)'
      review: not-applicable
      via: []
  markdown:
    all:
      reason: apply_pagination only slices when limit is explicitly set (markdown/results.py:103-118);
        confirmed live, output identical
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      review: unreviewed
      via: []
    until:
      review: unreviewed
      via: []
    verbose:
      reason: 'any unrecognized ?key=value becomes a frontmatter-field filter (generic
        filter mechanism); confirmed live: ?verbose=1 looked for docs with frontmatter
        field ''verbose'', matched 0 of 69 -- wiring --verbose here would collide
        with any doc that has a real verbose: frontmatter key (adapter.py:282)'
      review: not-applicable
      via: []
  mysql:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      reason: queries a live MySQL connection (adapter.py:328), no filesystem walk
      review: not-applicable
      via: []
    since:
      reason: live server health/performance introspection; UNIX_TIMESTAMP() computes
        one scalar (server_start_time), not a per-item timestamped list to filter
      review: not-applicable
      via: []
    until:
      reason: live server health/performance introspection; UNIX_TIMESTAMP() computes
        one scalar (server_start_time), not a per-item timestamped list to filter
      review: not-applicable
      via: []
    verbose:
      reason: no query-param whitelist or verbose-shaped hook found in code (adapter.py:328);
        code-reading only, no live MySQL server on this dev machine (same caveat as
        BACK-1382). Distinct from BACK-1382 (mysql's all-gap is a hardcoded SQL LIMIT
        N, a count issue)
      review: not-applicable
      via: []
  nginx:
    all:
      via:
      - adapter-reads-args
    respect_gitignore:
      reason: globs nginx config files under /etc/nginx-style paths (adapter.py:953,
        glob.glob at :236,239,267,269,415) -- infra config, not a git-tracked checkout
      review: not-applicable
      via: []
    since:
      reason: vhost/config inspection with no date field in get_structure()'s output;
        the one date-shaped field (last_seen) belongs to a separate diagnose subcommand
        path unreachable from the nginx:// URI
      review: not-applicable
      via: []
    until:
      reason: vhost/config inspection with no date field in get_structure()'s output;
        the one date-shaped field (last_seen) belongs to a separate diagnose subcommand
        path unreachable from the nginx:// URI
      review: not-applicable
      via: []
    verbose:
      via:
      - adapter-reads-args
  overview:
    all:
      via:
      - routing-special
    respect_gitignore:
      via:
      - declared
    since:
      review: unreviewed
      via: []
    until:
      review: unreviewed
      via: []
    verbose:
      via:
      - routing-special
  pack:
    all:
      reason: budget-constrained curation (?budget=) is the adapter's purpose; --all
        would contradict it
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: candidate file scoring/budget selection has no per-item date field;
        the module's 'since_ref' concept is the MCP pack tool's own git-ref diff parameter,
        an unrelated feature sharing a keyword
      review: not-applicable
      via: []
    until:
      reason: candidate file scoring/budget selection has no per-item date field;
        the module's 'since_ref' concept is the MCP pack tool's own git-ref diff parameter,
        an unrelated feature sharing a keyword
      review: not-applicable
      via: []
    verbose:
      via:
      - declared
  patches:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      reason: static scan of test files for mock.patch() call sites, grouped by target/test/file;
        no date field, no git integration
      review: not-applicable
      via: []
    until:
      reason: static scan of test files for mock.patch() call sites, grouped by target/test/file;
        no date field, no git integration
      review: not-applicable
      via: []
    verbose:
      reason: 'CLI_QUERY_FLAGS declares only ''all''; get_structure(**kwargs) has
        no detail-level concept; confirmed live: ''Unknown query param verbose'' warning
        (adapter.py:26)'
      review: not-applicable
      via: []
  python:
    all:
      reason: get_packages_list has no [:N] slicing; confirmed live, output identical
      review: not-applicable
      via: []
    respect_gitignore:
      reason: lists installed interpreter packages (adapter.py:148, get_packages)
        -- reads Python environment metadata, not a project directory walk
      review: not-applicable
      via: []
    since:
      reason: build_date/source_mtime/pyc_mtime are single-scalar environment facts
        or single-file staleness checks, not a per-item filterable field in a listable
        result
      review: not-applicable
      via: []
    until:
      reason: build_date/source_mtime/pyc_mtime are single-scalar environment facts
        or single-file staleness checks, not a per-item filterable field in a listable
        result
      review: not-applicable
      via: []
    verbose:
      reason: 'no query-string parsing at all, same shape as env://; confirmed live:
        ''?verbose=1'' errors as an unknown element (adapter.py:148). sys.flags.verbose
        read elsewhere is the interpreter''s own -v flag, unrelated to reveal''s --verbose'
      review: not-applicable
      via: []
  reveal:
    all:
      reason: self-introspection metadata (adapters/rules/analyzers) is small and
        fixed-size, not paginated
      review: not-applicable
      via: []
    respect_gitignore:
      reason: returns fixed self-introspection metadata (adapter.py:78, registered
        adapters/rules/analyzers) -- no filesystem walk over a target path
      review: not-applicable
      via: []
    since:
      reason: self-introspection of reveal's own config/capabilities (adapter.py:78),
        no date field anywhere
      review: not-applicable
      via: []
    until:
      reason: self-introspection of reveal's own config/capabilities (adapter.py:78),
        no date field anywhere
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(**kwargs) has no query validation; confirmed live: ?verbose=1
        output byte-identical, no warning, no detail hook exists (adapter.py:78)'
      review: not-applicable
      via: []
  sqlite:
    all:
      reason: schema/health inspection only, no row-listing feature exists to cap
      review: not-applicable
      via: []
    respect_gitignore:
      reason: operates on one .db file via sqlite3 (adapter.py:112,160), no directory
        walk
      review: not-applicable
      via: []
    since:
      reason: returns DB-level metadata and table schema (columns/indexes), not row-level
        query results -- no WHERE-style filtering surface exists to bind since/until
        to
      review: not-applicable
      via: []
    until:
      reason: returns DB-level metadata and table schema (columns/indexes), not row-level
        query results -- no WHERE-style filtering surface exists to bind since/until
        to
      review: not-applicable
      via: []
    verbose:
      reason: 'get_structure(**kwargs) has no query validation; confirmed live against
        a real 30-row fixture db: ?verbose=1 output byte-identical, no warning, no
        per-table/column detail expansion (adapter.py:435)'
      review: not-applicable
      via: []
  ssl:
    all:
      reason: single-certificate detail view; no list-of-many-certs output type exists
      review: not-applicable
      via: []
    respect_gitignore:
      reason: probes one host's certificate over the network (adapter.py:402); the
        only glob.glob use locates an nginx config path for a cert-file hint, not
        a project tree walk
      review: not-applicable
      via: []
    since:
      reason: certs carry not_before/not_after and a domain scan can return multiple
        results, but no since/until filter code exists anywhere in ssl/*.py, and the
        semantic is ambiguous for a live cert-validity check (unlike overview/markdown/autossl,
        there's no partial implementation to build on)
      review: not-applicable
      via: []
    until:
      reason: certs carry not_before/not_after and a domain scan can return multiple
        results, but no since/until filter code exists anywhere in ssl/*.py, and the
        semantic is ambiguous for a live cert-validity check (unlike overview/markdown/autossl,
        there's no partial implementation to build on)
      review: not-applicable
      via: []
    verbose:
      reason: 'extra cert detail (chain, subject, etc.) is already reachable via separate
        ssl://host/<element> paths, not a verbose flag; confirmed live: ''Unknown
        query param verbose'' warning (adapter.py:402)'
      review: not-applicable
      via: []
  stats:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      via:
      - declared
    until:
      review: unreviewed
      via: []
    verbose:
      reason: 'CLI_QUERY_FLAGS declares only respect_gitignore/all; get_structure''s
        summary_only already defaults to the fuller (non-summarized) shape -- nothing
        left for verbose to add; confirmed live: ''Unknown query param verbose'' warning
        (adapter.py:183-186,438-446)'
      review: not-applicable
      via: []
  surface:
    all:
      reason: top defaults to None (surface.py:291,336) -- already unclamped; confirmed
        live, output identical
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: attack-surface/taxonomy rule matching against source; no date field
        anywhere in the file
      review: not-applicable
      via: []
    until:
      reason: attack-surface/taxonomy rule matching against source; no date field
        anywhere in the file
      review: not-applicable
      via: []
    verbose:
      reason: 'render_structure(result, format, top) has no verbose param, no detail-level
        concept; confirmed live: ''Unknown query param verbose'' warning (surface.py:419)'
      review: not-applicable
      via: []
  testability:
    all:
      via:
      - declared
    respect_gitignore:
      via:
      - declared
    since:
      reason: testability scoring (mock pressure, coupling); no date field in the
        file
      review: not-applicable
      via: []
    until:
      reason: testability scoring (mock pressure, coupling); no date field in the
        file
      review: not-applicable
      via: []
    verbose:
      reason: 'CLI_QUERY_FLAGS declares only ''all''; render_structure(result, format)
        has no verbose param; confirmed live: ''Unknown query param verbose'' warning
        (testability.py:132)'
      review: not-applicable
      via: []
  trace:
    all:
      reason: --all conventionally lifts a RESULTS cap (top=N/limit=N); trace's cap
        (trace.py:26-30, default depth=2) is a BFS TRAVERSAL-DEPTH bound, a different
        axis -- 'show all' would mean 'traverse arbitrarily deep', which risks a runaway
        walk on a large or cyclic call graph (sibling code in calls/adapter.py:412
        deliberately clamps depth for the same reason). --depth=N is already the documented,
        working escape hatch for a user who wants more.
      review: not-applicable
      via: []
    respect_gitignore:
      via:
      - declared
    since:
      reason: call-graph trace from an entry point; no date field anywhere in the
        file
      review: not-applicable
      via: []
    until:
      reason: call-graph trace from an entry point; no date field anywhere in the
        file
      review: not-applicable
      via: []
    verbose:
      reason: 'render_structure(result, format) has no verbose param, no detail-level
        concept; confirmed live: ''Unknown query param verbose'' warning (trace.py:266)'
      review: not-applicable
      via: []
  xlsx:
    all:
      review: unreviewed
      via: []
    respect_gitignore:
      reason: operates on one spreadsheet file (xlsx.py:599,647), no directory walk
      review: not-applicable
      via: []
    since:
      reason: spreadsheet cell/sheet introspection; data-dependent, no canonical date
        field the adapter itself recognizes -- same class as json/sqlite
      review: not-applicable
      via: []
    until:
      reason: spreadsheet cell/sheet introspection; data-dependent, no canonical date
        field the adapter itself recognizes -- same class as json/sqlite
      review: not-applicable
      via: []
    verbose:
      reason: 'render_structure(result, format) has no verbose param; confirmed live:
        ''Unknown query param verbose'' warning (xlsx.py:30). Distinct from BACK-1382
        (xlsx''s all-gap is analyzer pre-truncation, a count issue)'
      review: not-applicable
      via: []
flags:
- verbose
- all
- since
- until
- respect_gitignore
paths:
- uri
